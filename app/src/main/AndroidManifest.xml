<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cormac.gym12m">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddExercises"/>
        <activity android:name=".GetAllExercisesActivity"/>
        <activity android:name=".GetAllWorkoutsActivity"/>
        <activity android:name=".Login"/>
        <activity android:name=".ExerciseList"/>
        <activity android:name=".Camera"/>
        <activity android:name=".Register"/>
        <activity android:name=".Add_Workouts" />
        <activity android:name=".MainMenu"/>
    </application>

</manifest>